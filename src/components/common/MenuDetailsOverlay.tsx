import React, { FC } from "react";

const MenuDetailsOverlay:FC = () => {
    return(
        <div className="login-wrapper overlay-wrapper">
            <div>

            </div>
        </div>
    )
};

export default MenuDetailsOverlay;